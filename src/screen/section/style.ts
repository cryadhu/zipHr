import { StyleSheet } from "react-native";

import { windowBackground } from "../../assets/colors";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: windowBackground,
    paddingBottom: 10,
  },
});
